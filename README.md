# DelQuant

Static public site for DelQuant, an NBA analytics and decision-support platform.

## Deployment

The site is designed for static hosting, including GitHub Pages. The custom domain is
declared in `CNAME` as `delquant.com`.
